# Targeted organic and quote recovery — release contract

This is a technical release packet, not a competing commercial monitoring register. The repository is public: customer records and business outcome evidence remain in the owner's private canonical organic-monitoring register.

## Scope

- Preserve the homepage, existing canonical URLs, redirects and recovered long-form sections.
- Add buyer-useful acrylic panel/cut-letter choices and lightbox formats, quote inputs and existing gallery proof.
- Connect the existing retail, wayfinding, exhibition and event journeys plus the blog/article. Hold new promotion of the calculator and 3D-printing page pending price/capability validation.
- Shorten the mobile quote introduction; retain phone access; add an optional HTTPS artwork/site-photo link. No file uploader, remote fetcher, new storage vendor or private data in analytics.
- Add prospective numeric Web Vitals in the existing database with an exact-origin/release/route contract, RLS, client-role revokes, bounded payloads and monotonic updates. This is measurement infrastructure, not evidence of a faster site.
- Provide a conservative operator-only existing-deal reconciliation command. No customer contact, automatic deal creation or advertising.

## Acceptance and release controls

The normal release runbook remains authoritative. Preserve both protected source ancestors and pass lockfile install, lint, typecheck, audit regressions, dependency audit, production build and Playwright before PR merge. The new telemetry migration must pass a rollback-wrapped PostgreSQL probe for RLS/permissions, numeric constraints, dedupe and stale-update rejection; it creates only the new telemetry table and trigger and starts with zero measurements.

Both Vercel Lane 0 phases must match the approved SHA and deployment. Hosted tests establish signed QA for customer journeys and collector suppression; synthetic normal-mode analytics and mocked success tests run only locally. No public synthetic enquiry or telemetry measurement is introduced. Signed QA still creates no CRM row, email or downstream delivery.

Customer acceptance: at 390×844, the name field fits inside the initial viewport; valid/invalid artwork links follow the validation contract; duplicate acceptance creates one success event; browser storage denial does not prevent submission. Acrylic/lightbox pages retain their canonical and useful sections and permit navigation through the relevant sector to the quote page. New links must be crawlable HTML anchors and the route audit must retain all existing protected paths.

## Outcome boundary

Google's existing indexed content and fresh search/CRM baselines belong in the private register. New indexing, ranking gains, genuine weekly enquiry recovery and field p75 are not release-gate pass criteria that can be claimed immediately. They require complete-date observations and adequate real visitor samples. Do not substitute lab scores, synthetic QA or database row existence for commercial recovery.
